packages:
- name: openstack-nova
  releases:
  - name: kilo
    repos:
    - branches: [kilo, f22]
      repo: fedora dist
  - name: juno
    repos:
    - branches: [juno, f21]
      repo: fedora dist
repos:
  fedora dist:
    fetcher: distgit
    options: {repo_base: 'git://pkgs.fedoraproject.org/'}
    title: Fedora dist-git @ pkgs.fedoraproject.org
